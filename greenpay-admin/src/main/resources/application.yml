server:
  port: 8081
#  servlet:
#    context-path: /admin
logging:
  level:
    com.github.binarywang.wxpay: error
    me.chanjar.weixin: error
    com.esiran.greenpay.pay.mapper: debug

spring:
  profiles:
    active: dev
  mvc:
    static-path-pattern: /**
delay_queue:
  redis:
    default_key: 'greenpay:delay_queue'
  consumer:
    enabled: true

shiro:
  loginUrl: /login
  successUrl: /home